# =======================
# ENVIRONMENT
# =======================
PORT=3001
NODE_ENV=development  # development | production

# =======================
# DATABASE (PostgreSQL/Supabase)
# =======================
DATABASE_URL=postgresql://user:password@localhost:5432/fcg_storage
# En producción con Supabase, SSL se habilita automáticamente

# =======================
# API KEYS (Autenticación)
# =======================
# IMPORTANTE: Generar keys seguras para producción (64 chars hex)
# Ejemplo: openssl rand -hex 32
API_KEY_MAIN=your-main-api-key-here
API_KEY_SECONDARY=your-secondary-api-key-here
# API_KEY_MAIN debe coincidir con STORAGE_SERVICE_API_KEY del backend principal

# =======================
# STORAGE (Archivos)
# =======================
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760  # 10MB en bytes
# MIME types permitidos (dejar vacío para usar defaults seguros)
# ALLOWED_MIME_TYPES=image/jpeg,image/png,application/pdf

# =======================
# THUMBNAILS (Imágenes)
# =======================
THUMBNAIL_WIDTH=300
THUMBNAIL_HEIGHT=300
THUMBNAIL_QUALITY=80

# =======================
# CORS (Orígenes permitidos)
# =======================
# En producción: solo dominios específicos
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# Ejemplo producción: CORS_ORIGINS=https://fcgapi.fundacioncarmengoudie.cl

# =======================
# RATE LIMITING (Protección DDoS)
# =======================
THROTTLE_TTL=60        # Ventana de tiempo en segundos
THROTTLE_LIMIT=100     # Máximo de requests por ventana (global)

# =======================
# SEGURIDAD (Producción)
# =======================
# En NODE_ENV=production:
# - Swagger docs deshabilitado
# - CORS estricto (solo orígenes listados)
# - SSL habilitado para PostgreSQL
# - Logging de intentos de auth fallidos

